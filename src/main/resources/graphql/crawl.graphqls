type Crawl {
    id: ID!,
    nameUrl: String!,
    status: Boolean!
}

extend type Query {
    findAllCrawl: [Crawl]
}

input CrawlInput {
    nameUrl: String!,
    status: Boolean!
}

extend type Mutation {
    addCrawl(input: CrawlInput): Crawl
}